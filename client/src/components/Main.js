import React from "react";

const Main = () => {
  return (
    <p></p>
    // will need link to the specific game like this path= '/game/:id' component={GameDescription} />
  );
};

export default Main;
